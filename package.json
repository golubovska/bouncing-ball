{
  "name": "bouncing_ball",
  "version": "1.0.0",
  "description": "My bouncing ball project",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "lite-server",
    "clean": "rimraf dist/",
    "copy-files": "copyfiles -u 1 src/**/*.html dist/",
    "build": "npm run clean && tsc && npm run copy-files"
  },
  "files": [
    "dist/**/*"
  ],
  "author": "Andrijana Golubovska <golubovskandrijana@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "connect-history-api-fallback": "^1.6.0",
    "copyfiles": "^2.4.1",
    "lite-server": "^2.6.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.3"
  },
  "dependencies": {}
}
